local M = {}

---@param theme Theme
---@param colors Colors
function M.getGroups(theme, colors)
    return {
        -- render-markdown, use the default
        -- Check `:h render-markdown-colors`
    }
end

return M
